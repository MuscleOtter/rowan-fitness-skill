# Validation and limits

## 1.18.0 selected 1.16.1 repairs

Carries forward the safe behavioral fixes from the 1.16.1 line: complete exact-text alternatives, truthful headers for outside programs, stronger pain holds, reminder-state uncertainty, and record portability. The 1.16.1 Claude review was interrupted by token exhaustion, leaving its full evaluation incomplete; its later cross-model pass also identified a material contradiction in the outside-program card rules. Rowan 1.18.0 fixes that contradiction, and its old evaluation pass rates are not reused here. Five fresh-context adversarial release rounds ran on this candidate, with a new `gpt-6-astra` medium-reasoning context for the final round. The rounds found and corrected migration wording, privacy-test markers, validation-count/link claims and release-note ambiguity; the candidate remains held only for the private author address in its Git ancestry and the resulting publication route. The current unit and package checks are static rule/package checks; they do not execute migration transitions or live-host coaching. The public-tree/ZIP privacy scan is clean. See the [1.18.0 validation receipt](validation-1.18.0.json).

## 1.17.0 migration-safe updates

Adds the non-blocking migration guide and links it from the entrypoint, memory, maintenance, setup, FAQ and model-facing index. The guide is a documentation/routing repair: it preserves older approved plans and personal records, maps new fields to explicit unknown or not-configured values, distinguishes a package update from a plan revision, and defines visible legacy plan versioning when an older record lacks a display version while preserving verifiable review evidence. It does not claim a live migration, a new reviewer run or an athlete outcome. The exact-final package check and relevant maintenance review remain required before calling this preparation a released package.

The candidate currently has 33 packaged skill files. `python3 -B -m unittest discover -s tools` passes 13 tests with one optional test skipped; `python3 tools/package.py --check` verifies exact source/archive bytes; `git diff --check` passes; and a focused migration regression check covers returning-mode routing, forward-schema protection, read-only replacement, canonical review labels, stable legacy/proposal IDs and dependent-artifact lineage. The skill-creator quick validator could not run because this environment does not have the optional `yaml` Python module. These are static/package checks, not a live host migration or reviewer run.

## 1.16.0 habits and follow-through

Adds Theo's handoff and habit loop as instructions only; no code paths change. On September 16, 2026, the package check, unit tests and internal link check passed. The three cited habit studies were confirmed in Crossref. The plan and the build each received an adversarial self-review by the AI model that wrote them, with the findings corrected. Independent maintenance review under rubric v1.2 is in progress. Pass 1, by a fresh-context reviewer, failed the release rule (weighted 8.694; one material finding about version-bound workout cards and eleven minor findings). Pass 2, by another fresh reviewer, failed (weighted 8.926; no material findings, five new minor ones, and verification scored 8.7 because no run of the revised candidate had been observed). Pass 3 met the release rule narrowly (weighted 8.869, with four dimensions at the 8.8 floor, no material findings and ten minor ones), using four fictional single-reply runs on its candidate as evidence. Across the three candidates, fresh evaluators found that exact card copying, holds that survive the athlete feeling better, and spacing checks improved with revision, while deferred clinician input, paraphrased short-session options and missing review routes recurred. Pass 3 advised moving those rules to where every pain report and review decision already looks, and shrinking the habits reference instead of adding wording; the current candidate does that. The exact-final check and runs on the current candidate are still required before release. No handoff, reminder, workout card, log sheet or habit loop has been exercised on a live host or with a real athlete.

## 1.15.1 reviewed maintenance fixes

This main-branch update contains the reviewed 1.15.0 fixes described below, with the skill version changed to 1.15.1. Three independent maintenance critique/revision rounds and a fresh exact-final source/package check completed under prospective rubric v1.2, with no material defect remaining. A separate independent evidence audit verified all eighteen synthetic role reports, input bindings, counts, N/A agreement, final score calculations and approved-text readbacks.

The version-only skill change is checked against the exact reviewed source; the rebuilt archive and documentation links are verified separately. Earlier execution remains bounded evidence for unchanged behavior, not a new 1.15.1 runtime test. No GitHub release or installation update is implied by a push to main. See the [public validation receipt](validation-1.15.1.json).

## 1.15.0 review depths (unreleased)

Adds standard and full review depths with conservative full-review triggers. On September 16, 2026, the package check, unit tests and internal link check passed. This changes review stage counts, which the project's own rules treat as an evaluated amendment requiring independent critique; the original v1.1 review identified a rubric conflict. A user-authorized prospective v1.2 amendment retains standard/full prescription depth while preserving three-pass independent skill maintenance review and all grading floors. Separate-context source critiques and bounded fictional routing probes are retained in the private review audit. A standard fictional sleep case completed all six fresh role reviews, revision, exact-final bindings, N/A agreement and release checks. Two rejected dispatches remain counted in its eight attempts; saved-artifact continuation completed without resetting the budget. A separately requested full-depth case completed all twelve fresh role reports across three critique/revision rounds and exact-final verification, with N/A agreement and release checks. Its last two reviews resumed from unchanged packets after an account usage interruption; no completed reviewer was reused or attempt count reset. Both cases passed all assigned floors and combined release criteria. These are actual Codex reviewer calls on fictional packets, not live athlete outcomes or a Claude installation test. Execution uses frozen references with a documented applicability comparison to four later wording/routing clarifications; it is not an exact-final runtime certification. Separate-context reviews do not establish independent model errors. These results concern the reviewed candidate incorporated into 1.15.1; no clinical equivalence, athlete outcome or measured savings is claimed.

## 1.12.1–1.14.0 onboarding and tester fixes (unreleased)

These versions change instructions, not code paths, except for a source-archive test and a clearer package-check message. On September 16, 2026, the package check verified 31 skill files and exact distribution bytes, 13 unittest methods passed with the optional parsed-Markdown test skipped, and every local skill link and anchor resolved. The new source-archive test fails without the `.gitattributes` rule and passes with it, and a simulated GitHub source download contains no nested ZIP. The onboarding greeting example measures 116 words against its 80–130 word limit.

The 1.14.0 fixes respond to one live tester session on Claude, read afterwards from documents the maintainer shared; it was not a controlled test and its details stay private. An adversarial review of 1.13.0 and 1.14.0 found 14 problems, all corrected, but it was performed by the same AI model that wrote the changes, so it is a self-review rather than independent evaluation. None of the changed behavior has been exercised on a live host, and the 5–10 minute setup estimate has not been timed.

## 1.12.0 visual handoff and style

The feature incorporates main through `a55436c`, including 1.10.1's conditional intake loading, context preflight and research-job verification. It adds a descriptive handoff contract, portable Rowan style guidance and one scoped CSS asset. The helper validates structure and renders Markdown tables; it is not an agent dispatcher, source verifier, clinical reviewer or JavaScript chart runtime.

On September 15, 2026, all **12 unittest methods passed**, including the optional parsed-Markdown round trip using an already installed Node/`marked` module. Packaging regressions verify bytecode exclusion, named CSS inclusion, and rejection of unapproved CSS/Python files, athlete records and symlinks. The skill structure validator passed. The rebuilt archive contains **31 files**, with exact source/archive bytes verified by `tools/package.py --check`.

A fresh-context tester exercised the local fallback after finding no nested delegation capability. Separately, the parent invoked a fresh Ellis consultation on a fictional six-day walking-duration log; the returned handoff validated, preserved five values and an unknown date, and described coverage as partial. These are real bounded agent executions, not a live health connection or complete clinical-board cycle. A separate source reviewer found no material defect, exercised 26 handoff and 108 malformed-field probes, and verified preservation of 1.10.1 behavior. No numeric maintenance or clinical grade is claimed.

A generated standalone browser preview was checked at 320, 360 and 736 pixels in Day and Night modes. All six checks had no page overflow, clipped SVG labels or label-label overlaps; the unknown date remained a gap between two line segments. Keyboard theme selection, pressed state and polite status update worked. Screenshots were visually inspected at phone and normal width. This tests one generated line/table example, not every possible label, metric or host.

The shipped style requires a short actual-output review, with a repair/recheck or readable fallback. A Codex in-message visual was delivered and positively acknowledged by the user; its sandboxed preview passed the same six width/theme checks, exact-value disclosure and keyboard activation. This is not a ChatGPT-app test.

The final-output review caught excessive procedural handoff text in the generated preview. The style guidance now distinguishes concise user-facing limitations from internal instructions and historical agent status. The preview was repaired while retaining the full handoff; all six width/theme checks and the keyboard check passed again. Subsequent Claude testing identified host button-style contrast and narrow-container spacing issues; the style guidance now explicitly checks inactive controls, axis-unit/top-tick separation and host menu clearance.

Claude evidence is a prompted-excerpt smoke test: relevant portable visual rules and the fictional Ellis rows were pasted into ordinary Claude web chat in Chrome, which generated a native in-message chart. Three follow-up revisions addressed inactive-button contrast, narrow heading/unit spacing, and bottom-axis spacing plus the user's request to hide the visible theme-status sentence. The final chart passed Day/Night label-clipping and label-overlap checks at normal width and 320/360px browser widths (actual embedded plots approximately 695/201/237px). Screenshots confirmed the final normal Day and narrow Night presentation. This is not first-pass success or a guarantee of arbitrary generated layouts.

The final exact-value disclosure preserved all six rows, including the unknown date; the chart retained its missing-day gap. Keyboard activation selected Day with the correct pressed state, and its polite status remained available but visually hidden. It is not an installed-ZIP loading/activation test or a Claude Code, Cowork, mobile-app or full clinical-board execution. No private athlete data, chat links, screenshots or development traces are published. See the [bounded validation receipt](validation-1.12.0.json) and [visual setup/test guide](VISUALS.md).

## 1.11.0 visualization candidate (not published or installed)

Ellis now has an on-demand table/chart contract and a Python-standard-library validator/Markdown-table helper. The core ships no JavaScript chart runtime or external service. Eight standard-library unittest methods exercise valid views, unknowns, planned/estimated/derived labels, invalid cells/shapes, chart mappings, text escaping/round trips and CLI rejection. A ninth test parses GFM output when an already installed `marked` module is supplied through `MARKED_MODULE`; otherwise it explicitly skips. These are software checks, not athlete outcomes, clinical-board execution or a native chart integration test. Run `python3 -B -m unittest discover -s tools` to reproduce them.

An independent source/package review found apostrophe-entity corruption and unintended URL autolinking in the initial helper, plus a missing readable-helper capability condition. The candidate now encodes data punctuation as character references, tests parsed cell text and absence of links/images/scripts, and requires both Python and the helper before execution. Initial passing substring tests did not detect those bugs; they are not evidence the earlier escaping was correct.

The deterministic package check verifies exact source/archive bytes. New visual behavior still needs live ChatGPT/Claude/mobile rendering and usability checks; host compatibility is capability-based, not a promise that installing the skill enables inline graphs. The existing review stages, role-selection policy, thresholds and memory semantics are unchanged. Historical scans below do not assess the new executable helper.

## 1.10.1 loading and host verification

**1.10.1 is a focused loading and host-verification repair.** First-use intake no longer unconditionally opens the 2,951-word personal-workflow reference; support requests still load it through the existing conditional routes. Context preflight counts coaching references and retained reports. Scheduled research checks web/source access in the job's actual environment and distinguishes configuration from observed execution. Roles, rubrics, review stages and permission boundaries are preserved. [1.10.1 validation record](validation-1.10.1.json).

Three fresh, bounded maintenance critiques found no demonstrated material defect; all fixed dimension floors passed. Final authored-adequacy score: 8.989/10. One reviewer exercised six fictional routing decisions, all passing; these are simulated outputs, not observed Claude execution. All 27 ZIP files match the final source, and 226 local links pass.

The directly required Step 1 references shrink from 9,190 to 6,239 words (32.1%). This scope excludes the entrypoint, other activation references, user history and tools; it is reproducible source-text accounting, not measured Claude tokens or a guaranteed full-turn saving. Actual loading, model tokenization, caching and conversation history affect cost. These changes do not establish live Claude scheduling, a full clinical recommendation cycle or athlete outcomes.

## Earlier version evidence

**1.10.0 is a pilot release.** Three distinct fresh source critics completed the fixed maintenance review. Pass 1 found a scientific-ledger save/conflict gap; the candidate was corrected. The second and third critiques found no material source defect. Final authored-adequacy score: **9.062/10**, every dimension at least 8.8. All 27 final ZIP entries match the reviewed source byte-for-byte; structure and local-link checks pass.

One independent agent completed 12 single-pass fictional immediate-response/routing scenarios, including four held-out cases and local fixture saves/read-backs. The final critic found those behaviors consistent with final instructions, while noting that the tester did not echo an exact candidate digest. This is bounded execution evidence, not exact-final runtime certification. Full clinical-cycle execution remains user-deferred; compaction, scientific-ledger concurrency and late specialist discovery have authored scenarios but no live execution in this phase. No longitudinal fitness outcome or measured token saving is claimed. See the [1.10.0 receipt](validation-1.10.0.json). Claude package compatibility was checked against current official documentation: the 27-file archive and its 184-character description satisfy the inspected format requirements. Local Codex and Claude Code installation checks matched all 27 reviewed release files. These establish folder integrity; separate ChatGPT Project and Claude account uploads, and live Claude board execution, remain unverified. [Verify each installation](SETUP.md#verify-each-installation).


**1.9.0 was an unreleased preparation checkpoint, superseded by 1.10.0.** Wren added sleep assessment, dated clinical evidence, optional Training Record fields and mandatory sleep review where specified. Three fresh external source critics completed the established passes; that candidate had no identified material instruction defect. Its last package/maintenance scores were 9.085/9.132, with every dimension at least 8.8. Those grades do not approve an athlete recommendation.

Twenty-one fictional immediate-response cases were exercised in two independent contexts. One initial research-date response failed; the catalog was corrected and a fresh original-source check verified the updated explanation. A separate local record test preserved old review history, a correction and a stopped tactic. The full 20-review sleep recommendation cycle did **not** complete: native capacity failures exhausted one cycle, and the standalone route produced an incomplete review followed by a model compatibility failure. No sleep advice was released. That 25-file candidate was checked by the third critic; its contracted fresh final delivery/usability review remained pending at that checkpoint. See the historical [1.9.0 preparation receipt](validation-1.9.0.json) for checksums, actual results and outstanding acceptance work. There was no standalone 1.9.0 publication or installation. Its source work was included in published 1.10.0; the deferred full clinical cycle remains unverified.

**1.8.2 repairs Claude workflow and review-capacity guidance.** Claude Code now has an explicit conditional route for scheduling, current-record access and delivery. Checkout requires concrete confirmation on Claude, or stays with the user where purchases are prohibited. Reviews budget context as well as calls, preserve complete reports and verified checkpoints, and check automatically loaded reviewer memory for prior verdicts. No live Claude scheduling, checkout, reviewer startup or compaction recovery is established by these instruction changes.

**1.8.1 improves nontechnical setup and everyday use.** The Claude app/Cowork route comes before terminal instructions; opening questions are shorter; Rowan introduces relevant specialists as they help and prepares complete records for saving and attaching. This is a presentation repair: grading, required reviewer coverage, permissions and memory rules remain intact. It has not been tested with a panel of real nontechnical users.

**1.8.0 adds a personalized support routine:** approved-workout delivery, brief check-ins, reminders and grocery help through actual authorized tools. Rowan keeps useful tactics, adjusts friction and stops unwanted support, including its actual jobs. The new 8.8 floor never lowers existing 9.0 or stricter fitness requirements. Native neutral reminders are distinguished from agent-run jobs that must recheck current authority and plans.

**1.7.0 adds explicit cardio and practical nutrition coaching, readable reviewer names, and native-first Apple Health setup.** The conditioning role and nutrition coverage are part of the same bounded review process. Health access remains conditional on actual tools and user permissions. This release does not establish live iPhone execution or athlete outcomes.

**1.6.0 adds history discovery and connection-aware setup.** Relevant authorized fitness conversations and available readers can supply intake context before repeated questions. Apple Health phone sharing, session access and background access are separate checks. Imports preserve source identity, deduplication, corrections and deletion exclusions; every receipt states actual save status. These are portable instructions for available host tools, not bundled device adapters.

**1.5.3 corrects provenance and update documentation.** The research reference now gives its original research date and first public package instead of implying it is the current package version. The corrected host maps are attributed to 1.5.1. Coaching instructions, host capabilities and the review protocol are unchanged from 1.5.2. Each scan below applies to its explicitly named target.

**1.5.2 is a metadata and validation-documentation update.** The author field uses the maintainer's GitHub no-reply address, so the author-format check now passes without exposing a personal email. The rebuilt package passes both structure validators and exact ZIP/source checks. Coaching behavior and host setup are unchanged from 1.5.1.

**1.5.1 corrects the 1.5.0 host layer after independent review.** That review found the ChatGPT route transferred no instructions, install commands that could not run as written, a demoted Cowork route, binding selection keyed to the wrong capability, and delegation modes that inherit context and therefore cannot supply an independent reviewer. All five are fixed here. 1.5.0 was merged but never released, so no published package carried them.

**1.5.0 was a host-adaptation release.** It changes the discovery description, names the tools each Claude and each ChatGPT/Codex surface actually supplies, and gives concrete install routes for Claude Code and Codex. Coaching policy, roles, rubric, thresholds, the review cycle and record semantics are unchanged from 1.4.1. Historical coaching reviews do not establish live execution on the changed host layer.

**1.4.1 was the first public release.** It removes first-user context, generalizes one intake example and adds public documentation and packaging. The coaching rules are inherited from the reviewed 1.4.0 package; the older score is not a fresh efficacy claim for this release.

| Evidence | What it establishes |
|---|---|
| Three independent 1.8.2 maintenance reviews | The first review found a material startup-memory contamination path despite non-fork invocation; it was fixed and a broken host-route anchor repaired. The next two fresh reviewers found no material authored defect. Final weighted score: 9.127, with all eight dimensions 8.9–9.3 under unchanged rubric v1.1. These assess authored adequacy, not live Claude execution or athlete outcomes. |
| 1.8.2 fictional response evidence | Five responses from a separate resumed worker cover session-lifetime limits, concrete checkout approval, prohibited purchases, seven-role recovery and lost exact evidence. Three additional responses from a fresh worker on the final bytes cover clean startup selection, rejection of an exposed reviewer and reconciliation of an uncertain dispatch. No expected replies were supplied; these are simulated decisions, not performed account actions or a complete fitness board. The final reviewer checked which earlier-probe behavior still applies after the startup fix. |
| 1.8.2 final package and host checks | The final reviewer independently matched all 23 source/ZIP files to the frozen manifest; both structure validators and local documentation links pass. Sixteen skill files, including fitness/maintenance rubrics, roles and core memory, remain byte-identical to 1.8.1. Official Claude documentation supports the conditional routes. NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.8.2.json). |
| 1.8.1 usability review and revision | A prospectively frozen six-dimension usability rubric found weaknesses in installation, plain language and handoff. After revision, the final separate reviewer scored all six dimensions 8.9–9.4 and all eight maintenance dimensions 8.9–9.4 (weighted 9.184). The final reviewer used a resumed context after the host's task limit prevented a fresh task; this is an authored-quality assessment, not fresh-context prescription-board approval. No material defect remained in the inspected scope. |
| 1.8.1 conversation evidence | A fresh separate agent generated five fictional first-use, quick-log, missing-record, unavailable-review and memory/grocery replies. After progressive introductions were added, the final reviewer generated a Rowan-only welcome and walked through later specialist introductions and a full-team request. These are limited AI-generated probes; the later responses were not blind or independent coaching runs. No real nontechnical participant, account action or fitness prescription was tested. |
| 1.8.1 final package and documentation checks | The final reviewer verified the exact 23-file source and ZIP. Both structure validators pass; local documentation links resolve. Fourteen core review, fitness, import, record-template and workflow files are byte-identical to 1.8.0. NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.8.1.json). |
| Three independent 1.8.0 maintenance reviews: 9.07, 9.178, 9.188 | Every dimension meets the new 8.8 floor under prospectively amended rubric v1.1; historical grades retain their original rubric. Revisions clarified logistics-only changes, native neutral reminders versus agent-run jobs, and capability-dependent setup. The final reviewer found no necessary changes and verified the exact 23-file source and ZIP. These are authored-quality judgments, not outcome estimates. |
| 1.8.0 workflow behavior evidence | A fresh agent generated responses to four fictional no-outreach, send-timeout, stop-failure and checkout fixtures without supplied expected answers. The second and third reviewers separately constructed eight and ten current-candidate scenarios. An isolated temporary-file check preserved a corrected record/stopped tactic and exposed an injected write failure. Mock decisions and file mechanics do not establish live delivery, purchasing, cross-host memory or athlete outcomes. |
| 1.8.0 host and package checks | Official Claude documentation and exposed Codex tool descriptions support conditional setup routes; no real account actions were performed. Both structure validators pass; all 23 ZIP members exactly match the reviewed source. NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.8.0.json). |
| Three independent 1.7.0 maintenance reviews: 8.77, 8.895, 8.84 | All eight dimensions meet the unchanged 8.5 floor; no material authored defect remains. Revisions reconciled role summaries and call-budget wording. The final reviewer verified the exact 22-file candidate, including the last role-name clarification. These grades assess authored adequacy, not real-world coaching efficacy. |
| 1.7.0 cardio, nutrition and recipe fixtures | Eighteen raw scenarios plus bounded additional probes cover intake, cardio/nutrition arithmetic, recipe yield, allergens/storage, complete role coverage, naming, full-team onboarding, memory and phone/reviewer boundaries. Three reviewers retained explicitly constructed walkthroughs; real arithmetic checks confirmed planted defects. These are not live athlete recommendations, cooked recipe tests or full seven-role board executions. |
| 1.7.0 package and scan checks | Both structure validators pass; all 22 archive members exactly match the independently reviewed source, and internal repository links resolve. NVIDIA Tier 1 reports 10 of 11 validators passing with security scanning incomplete. See the [scan receipt](validation-1.7.0.json). |
| Three independent 1.6.0 maintenance reviews: 8.84, 8.77, 8.84 | Authored adequacy under the unchanged eight-dimension rubric, with every final dimension at least 8.5. Reviews did not measure athlete outcomes or prove connector execution. Native Claude findings prompted explicit local-transcript support and paired import/save receipts; the final review also corrected current versus legacy Claude settings. |
| 1.6.0 native Claude Code smoke checks | Two bounded runs inspected real session capabilities and reasoned through synthetic history, connection, save-failure and scheduler cases. Findings were fixed or dispositioned, then the final independent review checked the revised instructions. No real history, Health or scheduling tools were exercised. The last native run used the pass3 candidate; later goal wording, chat-only completion, delegation-link and settings corrections received scoped review, not another native run. |
| 1.6.0 final setup regression and isolated save/replay/restore | Eighteen simulated response cases covered identity, history, missing access, pagination, late edits, failed jobs and chat-only completion; no material defect was reproduced. Separately, a test-only harness wrote and read back a fictional record: mirrored imports yielded two unique workouts/70 minutes, replay added nothing, and a separate reader process restored the same result. An injected failed-save stub left the prior boundary unchanged after one retry. This exercises mechanics; the harness is not a shipped connector, a fresh AI conversation, or a concurrency guarantee. |
| 1.6.0 export and static checks | Both structure validators pass; all 19 ZIP members exactly match the reviewed source. NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.6.0.json). |
| Three independent recursion reviews of 1.3.0–1.4.0: 8.65 → 8.84 → 8.84 | The final authored package met the fixed rubric: every dimension at least 8.5; no material defect identified. Initial missing evidence and a baseline-portability issue were addressed. |
| A historical synthetic non-cut plan, 16 fresh reviewer calls, final fitness score 9.13 | A bounded independent-review workflow completed, found defects, revised and checked the exact final candidate. |
| Synthetic maintenance and final-behavior scenarios with actual local file operations | Pending outcomes, corrections, unavailable data, automatic on-use checks, candid pushback and acceptance of new facts were exercised within their stated test scope. |
| 1.4.1 package and export checks | Both skill validators passed; 47 internal links across the public docs and skill resolved. All 18 ZIP members exactly match source, including the license. These checks do not run a coach. |
| 1.5.0 export checks | 51 internal links across the public docs and skill resolved; all 18 ZIP members exactly match source. These checks do not run a coach. |
| 1.5.1 recheck | All five earlier findings were resolved; both structure validators and 53 internal links passed. An anonymous download delivered the exact 18-file source package. |
| 1.5.2 metadata and export checks | Both structure validators pass; all 18 ZIP members match source. NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.5.2.json). |
| 1.5.3 provenance and export checks | Both structure validators pass; all 18 ZIP members match source. The backup example passes repeated-use checks in zsh and bash. Fresh NVIDIA Tier 1 reports 10 of 11 validators passing, with security scanning incomplete. See the [scan receipt](validation-1.5.3.json). |

Real athlete outcomes, a full five-role cut execution, live device imports, live Claude.ai upload/search, real scheduled maintenance, phone delivery, grocery purchases and competing background writers have not been established by these tests. The host tool maps introduced in 1.5.1 and extended in later versions describe documented capabilities and the locally observed checks noted above; a complete live athlete board run through Claude Code subagents or Codex delegation has not been recorded here; the 1.15.0 standard case above is a completed fictional workflow. The 1.9.0 attempt adds two actual fresh pass1 reviews but does not complete a board. Separate AI contexts do not guarantee independent models or independent errors.

The historical NVIDIA Tier 1 assessment of 1.8.2 remains **incomplete**. Author-format governance passes; SkillSpector's report-format incompatibility still prevents security-scan completion. Ten of eleven validators passed. The separate 83.2/100 quality heuristic is not the independent rubric score or a security verdict. No clean security certification is claimed. Some passing checks have no executable code to assess in this instruction-only package.

**There are two repositories in a scan.** That scan target is [Rowan v1.8.2](https://github.com/MuscleOtter/rowan-fitness-skill/releases/tag/v1.8.2). The evaluator is [NVIDIA/SkillEvaluator at `ff349e0d9f03868fc27d1e2bbd62eb849cba66c9`](https://github.com/NVIDIA/SkillEvaluator/commit/ff349e0d9f03868fc27d1e2bbd62eb849cba66c9). That commit exists in NVIDIA's repository; it is not a Rowan commit. The [scan receipt](validation-1.8.2.json) records the tested package checksum, evaluator identity, configuration and per-validator outcomes without publishing local paths or private traces. The earlier 1.5.1 target was [Rowan commit `9718dde`](https://github.com/MuscleOtter/rowan-fitness-skill/commit/9718ddefa9b27b0428dd8529bb8dcd2761adb888), assessed with that same NVIDIA revision.

The package consists of instructions and templates. It does not install device access, collect telemetry or supply a scheduler. Any connected assistant still operates under its own data handling and permissions. AI role names confer no professional credentials or guaranteed result.

Private development traces and athlete-style fixtures are intentionally not published. This page is a maintainer's bounded summary, not a publicly reproducible clinical evaluation. Public scenario specifications are in [verification.md](../skills/fitness-review-board/references/verification.md); repeatable export checks are described in [CONTRIBUTING.md](../CONTRIBUTING.md).
